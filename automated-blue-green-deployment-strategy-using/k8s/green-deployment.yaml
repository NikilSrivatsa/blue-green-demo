apiVersion: apps/v1
kind: Deployment
metadata:
  name: blue-green-demo-green
  namespace: blue-green-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: blue-green-demo
      color: green
  template:
    metadata:
      labels:
        app: blue-green-demo
        color: green
    spec:
      containers:
        - name: app
          image: docker.io/your-dockerhub-user/blue-green-demo:initial
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: APP_COLOR
              value: green
            - name: APP_VERSION
              value: initial
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
