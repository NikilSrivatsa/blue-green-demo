apiVersion: v1
kind: Service
metadata:
  name: blue-green-demo-green
  namespace: blue-green-demo
spec:
  type: ClusterIP
  selector:
    app: blue-green-demo
    color: green
  ports:
    - name: http
      port: 80
      targetPort: 8080
